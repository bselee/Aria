# Dependencies
node_modules/

# Testing
coverage/

# Build / Production
build/
dist/
.next/
.aria-cache/
tsconfig.tsbuildinfo
.tsbuildinfo.cli

# ─── Environment & Secrets ───────────────────────
.env
.env.local
.env.*.local
.env.local.corrupted
token*.json
*token*.json
*credentials*.json
credentials.json

# ─── Logs ─────────────────────────────────────────
logs/*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
output.log
output*.txt
dev.log

# ─── PM2 ──────────────────────────────────────────
.pm2/

# ─── IDEs ─────────────────────────────────────────
.idea/
.vscode/
*.swp
*.swo

# ─── OS ───────────────────────────────────────────
.DS_Store
Thumbs.db

# ─── Scratch / Temp Files ─────────────────────────
# All temp scratch files and one-off debugging scripts
scratch/
tmp/
tmp_*
# Assessment-discovered scratch (2026-08): temp debug scripts + sticker rabbit hole
scratch-*.ts
scripts/_tmp_*
scripts/_diag-*.ts
scripts/sticker-*.py
scripts/compare-sticker-pricing.ts
scripts/replay-split-rows.ts
scripts/summarize-sticker-report.py
test-*.ts
test_*.ts
probe-*.ts
probe-*.js
debug-*.ts
debug-*.js
force-*.ts
reply-slack.ts
scripts/scratch/
typecheck_output.txt
check_logs.ts
uline-*.csv
uline-*.json
uline-output.txt

# ─── Worktrees ────────────────────────────────────
.worktrees/
worktrees/

# ─── Scraper browser profiles (auth cookies, never commit) ───
.basauto-profile/
.basauto-session.json
.uline-profile/
.uline-session.json
chrome-profile/
AppData/

# ─── AI Agent Configs (catch-all) ─────────────────
# Only .agents/ and .firecrawl/ are tracked for this project.
# All other AI tool config dirs are ignored.
.adal/
.augment/
.claude/
.cline/
.clinerules
.codebuddy/
.commandcode/
.continue/
.crush/
.cursor/
.cursorrules
.factory/
.goose/
.iflow/
.junie/
.kilocode/
.kiro/
.kode/
.mcpjam/
.mux/
.neovate/
.openhands/
.pi/
.pochi/
.qoder/
.qwen/
.roo/
.trae/
.vibe/
.windsurf/
.windsurfrules
.zencoder/

# Local SQLite WAL/SHM (transient, change every write)
aria-local.db
aria-local.db-shm
aria-local.db-wal

# Playwright MCP logs
.playwright-mcp/

# Dev-server log dumps (developer-local, never useful in repo)
tmp-next-dev-*.err.log
tmp-next-dev-*.out.log

# Local scraped data — regenerated each run (timestamped scrapedAt field)
purchase-requests.json
purchases-data.json

# External MCP tool dir — intentionally not in this repo
tools/excel-mcp-server/

# Stray screenshots at repo root (smoke-test artifacts)
/*.png
tmp-*.ts
__pycache__/
data/downloads-archive/
.git-rewrite/
retry_at=*
# Daily DB backups
backup/
# PG health watchdog log
data/pg-health.log
# Live bot state, changes every watch tick
data/scans-watcher-state.json

# ─── Local-only operational artifacts (2026-07-28 triage) ────────
# PID sentinel written by src/lib/persistence/pid-guard.ts at every boot.
logs/*.pid
# Daily SQLite/PG backup dirs + ad-hoc pre-change file backups. Multi-GB.
backups/
.backups/
*.bak
# Vendored postgrest.exe + libpq DLLs — a 79 MB third-party binary drop.
bin/
# Generated purchasing/label report deliverables — regenerated on demand.
reports/
# Stray empty SQLite file; the real sidecar is aria-local.db (already ignored).
local.db
# Locally archived invoice/statement PDFs pulled from Gmail/Drive. Business
# documents, not source. NOTE: one statement under
# local/storage/statement_artifacts/FedEx/2026-07-15/ is already tracked from
# before this rule and stays tracked — git ignores only untracked paths.
local/storage/INVOICE/
local/storage/ap_invoices/
local/storage/statement_artifacts/
# Desktop/research drops — never commit (pitch decks, vendor PDFs, OCR pages, etc.)
# Whole tree: subdirs like ferticell-*-pages/ were leaking past *.png globs.
.hermes/desktop-attachments/
# Split-commit scratch dir
.tmp-split/
# One-off SKU/stock probe scripts from ad-hoc investigations. Kept on disk for
# the author's convenience but deliberately untracked: they hardcode local paths
# (e.g. C:\Users\...\Downloads\MyOrderHistory.csv) and duplicate the durable
# probes already tracked under src/cli/ (check-uls455.ts, check-demand.ts, ...).
# Promote one by `git add -f` if it ever earns a permanent home.
_check_*.ts
src/cli/check-S4551.ts
src/cli/check-S4551-detail.ts
src/cli/check-ULS455-stock.ts
src/cli/forecast-ULS455-S12610.ts
src/cli/surepack-all-range.ts

# LTL Select probe scratch (may contain tokens)
.tmp-ltl/


# ─── 2026-08-06 dirty-tree triage ─────────────────
# Local plans scratch (tracked plans stay tracked; only untracked stay out)
# Root/runtime junk — never source
nul
backups-archive-*.tar.gz
OA-Assessment-*.pdf
public/OA-Assessment-*.pdf
# Runtime data not covered by earlier data/* rules
data/billcom-error.png
data/billcom-profile/
# logs: only .log was ignored; pid/reaps are runtime too
logs/*.pid
logs/*.reaps.json
# Sandbox drops under local/storage
local/storage/SANDBOX/
# Ad-hoc box/SP/Uline forecast probes (hardcoded Downloads paths)
src/cli/black-friday-forecast.ts
src/cli/two-week-S4796.ts
src/cli/scan-close-calls.ts
src/cli/boxes-incoming.ts
src/cli/boxes-on-order.ts
src/cli/check-sp12126.ts
src/cli/check-sp-variants.ts
src/cli/check-stock-level.ts
src/cli/find-sp-po.ts
src/cli/sp-old-vs-new.ts
src/cli/sp-sku-po-audit.ts
src/cli/verify-sp-skus.ts
src/cli/verify-po-lines.ts
# One-off probe scripts
scripts/_precision_check_ap.ts
scripts/_probe-gmail.ts
scripts/_probe-gmail-deep.ts
# Duplicate of tracked scripts/warm-purchasing-cache-cron.mts
scripts/warm-purchasing-cache.mjs

# Shell warm-up capture (13-byte "EXIT CODE: 0" artifact, never useful in repo)
_warm_output.log

# ── 2026-08-13 triage (uncommitted-work-triage) ─────────────────────────────
# One-off invoice-schema probes (root); durable equivalents live in scripts/
.invoice-*.mjs
# Scratch probe/receipt-sync experiments from the 3-way match work
_probe-*.ts
_run-receipt-sync.ts
_validate-uom.ts
_apply_pdf_changes.py
# Cron warm-up shell artifact
warm-cache.log
# One-off DB repair scripts (already ran against live PG; promote with
# `git add -f` only if re-run is ever needed)
scripts/ap-gate-live-counts-pg.mjs
scripts/ap-gate-live-counts.ts
scripts/resolve-stuck-ap.ts
scripts/close-phantom-prefixed-pos.ts
scripts/fix-null-vendor-pos.ts
# Kanban worker scratch workspaces (board owns them, not the repo)
.hermes/kanban/
.hermes/plans/code-review-result-temp.json
