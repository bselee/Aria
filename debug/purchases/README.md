This folder holds the curated screenshots worth keeping for future reference.

Kept samples:
- `purchases-Sign_In.png` (login/credential prompt for the guidance site)
- `purchases-Purchases.png` (overview dashboard showing totals)
- `purchases-ULINE7.png` (example vendor card with urgency/fields)
- `purchases-loaded.png` (typical filtered view after the site finishes loading)
- `purchases-Overdue_History.png` (tab showing overdue guidance context)

All other screenshots and transient captures remain archived outside Git to avoid clutter while still being easy to recover if needed.

Sample data:
- `purchases-data.sample.json` is the cleaned capture used for guidance comparison.

Debug CLIs live in `debug/cli/` so they are clearly prototypes and won’t be confused with production tooling.
