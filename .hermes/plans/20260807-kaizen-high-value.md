# Kaizen 2026-08-07 — high-value next

See vault `Aria/Kaizen-2026-08-07.md` for full pass.

Must-do shortlist:
1. ~~AP sparse-window catch-up after bot restart~~ → `src/lib/ops/ap-boot-catchup.ts` + start-bot wire
2. ~~Unit test po-finale-sync enqueued===0 on unchanged~~ → `po-cache-change.ts` + tests
3. Optional: keepalive revive test off-hours (hold — do not stop live gateway in biz hours)
4. ~~ingest-triage on every chat attach~~ → AGENTS.md habit note
5. ~~Boot-grace for stale_cron false red~~ → control-plane + start-bot + migration 20h ap-polling
6. Billtrust when freight work resumes (still hold)

Do not: bulk Downloads vault, more LLM watchdogs, Aria-polices-Aria.
